# 修改记录

时间：2026-09-04
范围：DataInfra-RedactionEverything 脱敏可逆性加固（A–F 六项）
状态：已完成，代码已通过 `compileall` 与 9 项针对性测试

## A. file_store 不落完整原始全文
- 内容：脱敏成功后清除 `file_store` 记录中的 `content`、`pages`，保留还原与对比所需的 `entities`、`entity_map`，并写入 `original_data_purged=true`。
- 目的：减少完整原文在数据库中的明文残留，同时保持“对比”和“按类别还原”可用。
- 涉及文件：`backend/app/services/redaction_orchestrator.py`

## B. 短时热保留 + 加密冷存储
- 内容：脱敏成功记录 `redaction_completed_at`；默认 1440 分钟（1 天）后，后台每 15 分钟扫描一次，将原始文件用独立 Fernet 密钥加密移入 `DATA_DIR/cold_storage`；`entities/entity_map` 同时加密为元数据侧车后从库内清除。
- 目的：满足“1 天以内自动清理”的保密时效性，同时保留加密原件供事后恢复。
- 涉及文件：`backend/app/core/config.py`、`backend/app/services/cold_storage.py`、`backend/app/services/retention_service.py`、`backend/app/main.py`、`backend/app/services/redaction_orchestrator.py`、`backend/app/services/redactor.py`
- 说明：冷归档后对比/还原会按需从冷存储解密取回；`ORIGINAL_RETENTION_MINUTES=0` 表示立即归档，`-1` 禁用自动归档。

## C. 修复表格/页眉页脚漏脱敏
- 内容：新增残余敏感值清理器，在文本替换前追加清理手机号、身份证、银行卡、邮箱、金额、百分比和已知地域词。
- 目的：即使 NER 漏检或表格/页眉页脚未命中，也不再把原文金额、占比、地名留在输出中。
- 涉及文件：`backend/app/services/redaction/residual_sanitizer.py`、`backend/app/services/redactor.py`、`backend/app/services/redaction/text_redactor.py`

## D. 占位符去掉类型与数量信息
- 内容：SMART、STRUCTURED、MASK 三种模式在加固开启时统一输出 `[已匿名]`，不再带实体类型和递增编号。
- 目的：降低成员推理（通过类型与数量判断存在性）的风险。
- 涉及文件：`backend/app/services/redaction/replacement_strategy.py`

## E. 关联字段联动脱敏
- 内容：残余清理器同步处理金额、百分比、占比和地域词，避免只脱敏正文而留下表格数字或地名线索。
- 目的：阻断通过“20.49% + 表格数值 + 邻近地名”上下文回填占位符。
- 涉及文件：`backend/app/services/redaction/residual_sanitizer.py`

## F. 输出最小化重写
- 内容：通过统一匿名占位符 + 残余敏感值清理实现最小化输出，减少可映射上下文。
- 目的：降低 AI 社工/上下文推断的还原成功率。
- 涉及文件：同 D、E

## 配置开关
- `PRIVACY_HARDENING_ENABLED=false`：默认关闭；H 项已回退 D，开启时仅保留 A、C、E 的残余清理，不再统一输出 `[已匿名]`。
- `ORIGINAL_RETENTION_MINUTES=1440`：控制 B，1 天后加密转冷存储；`0` 立即归档，`-1` 禁用。
- `COLD_STORAGE_DIR=`：冷存储目录，默认 `DATA_DIR/cold_storage`。
- 冷存储密钥独立存放于 `DATA_DIR/cold_storage.key`（0600）。
- 开关已同步写入 `.env.example`。

## 验证
- `python -m compileall` 通过。
- `pytest tests/test_tenant_config_isolation.py tests/test_has_service_single_batch_20260902.py tests/test_closed_loop_recognition_20260902.py`：9 passed。
- 行为抽样（H 项回退后）：SMART/STRUCTURED/MASK 分别返回 `[机构名称一]` / `<组织[001]>` / `张*` 类原始结果；不再统一返回 `[已匿名]`。

## G. 文本 NER 切换为本地 llama.cpp（无 API Key）
- 内容：根目录 `.env` 中 `HAS_TEXT_RUNTIME` 改为 `llamacpp`，新增 `HAS_LLAMACPP_BASE_URL=http://127.0.0.1:8080/v1` 和本地 GGUF 路径，清空 `HAS_TEXT_API_KEY` / `HAS_TEXT_EXTERNAL_BASE_URL`。
- 目的：适配本地 llama-server 部署，移除外部千问服务和密钥依赖。
- 涉及文件：`.env`、`.env.example`

## H. 还原失败修复：回退统一 `[已匿名]`，恢复三模式脱敏
- 时间：2026-09-04 15:00
- 内容：删除 `PRIVACY_LABEL = "[已匿名]"` 及 `replacement_strategy.py` 中对 SMART/MASK/STRUCTURED/CUSTOM 的统一覆盖逻辑，恢复三种匿名化模式各自的原始替换结果；`PRIVACY_HARDENING_ENABLED` 默认改为 `false`；`PURGE_ORIGINAL_UPLOADS_AFTER_REDACTION` 保持默认 `false`。
- 目的：修复强隐匿化导致脱敏后无法被“脱敏还原”正确识别、对比接口返回 500 的问题，恢复可逆还原能力。
- 涉及文件：`backend/app/services/redaction/replacement_strategy.py`、`backend/app/core/config.py`、`.env.example`
- 说明：SMART 恢复 `[机构名称一]` 类编号，STRUCTURED 恢复 `<组织[001]>` 类结构化标签，MASK 恢复 `张*` / `138****5678` 类掩码。冷存储方案 B 仍保留，到期加密归档后按需解密恢复。
- 验证：`.venv` Python `compileall` 通过；三模式抽样输出已确认不再出现 `[已匿名]`；后端以 `.venv` 重新启动，`GET /health` 返回 200。
## I. 三轮闭环识别提升为基准版本
- 时间：2026-09-21
- 内容：`backend/app/main.py` 直接安装 20260902 闭环 overlay（HaS 单批调用、`run_hybrid_ner` / `run_default_ner` 闭环包装、`/ner/closed-loop`、`/ner/hybrid-20260902`、`/vision-20260902` 路由，以及 SPA 耗时 UI 注入），使 `app.main:app` 本身就是三轮闭环版本。
- 兼容：`app/main_20260902.py` 简化为同一 app 的别名，避免重复挂载路由；旧命令 `uvicorn app.main_20260902:app` 仍可用，指向同一个 app。
- 目的：消除“旧入口＝最初版本、日期化入口＝闭环”的双版本状态，`npm run dev`、`npm run dev:local`、裸 `uvicorn app.main:app`、部署脚本走同一条链路都是三轮闭环。
- 回退开关：`CLOSED_LOOP_ENABLED=false` 恢复单次识别；`CLOSED_LOOP_MAX_ROUNDS`（1-3）控制轮数上限。
- 涉及文件：`backend/app/main.py`、`backend/app/main_20260902.py`、`frontend/index.html`、`.env.example`
- 验证：`py_compile` 通过；`uvicorn app.main:app` 实测 `GET /api/v1/files/{id}/ner/closed-loop` 返回 401（路由存在；改动前为 404），启动日志出现 `Closed-loop recognition runtime overlay 20260902 installed`，`/` 返回的 HTML 含耗时 overlay 脚本；全量 `pytest` 565 passed / 11 failed，失败集合与改动前完全一致（8 项 20260902 RED 测试 + 3 项既有 vision 失败）。
## J. 登录失败修复：HTTP 访问的 cookie Secure 开关落地
- 时间：2026-09-21
- 现象：http://localhost 登录后报 Authentication session was not established.（前端 auth-context 会话复核失败），写接口报 403 Missing CSRF token。
- 根因：DEBUG=false 时 access_token / csrf_token 的 cookie 带 Secure（`secure=settings.COOKIE_SECURE and not settings.DEBUG`，COOKIE_SECURE 默认 True），纯 HTTP 下被浏览器静默丢弃 → 登录 POST 本身 200，但会话 cookie 不落地。
- 修复：根 `.env` 增加 `COOKIE_SECURE=0`；`.env.example` 认证段落补充说明；`docs/本地部署与启停指南.md` 第九节增加故障条目。HTTPS 部署保持 1。
- 验证：隔离 DATA_DIR 实测 setup/login 后 `/api/v1/auth/status` authenticated=true（同流程在 COOKIE_SECURE=1 时为 false）；全量 pytest 565 passed / 11 failed，与改动前一致。


