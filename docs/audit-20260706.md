# RedactionEverything 企业级交付审计报告

- 日期：2026-07-06　审计范围：全仓（backend/frontend/scripts/deploy/e2e）+ 双卡 5090 生产运行态（只读）
- 基准架构：OCR + PaddleOCR-VL + HaS-NER + GLM-4.6V-Flash-FP8 + YOLO（HaS_Image）五件套（Tracy 定板，HaS-NER 与 YOLO 为必保组件）
- 分级：**P0**=交付阻塞/数据风险　**P1**=企业级必修　**P2**=改进项；工作量 S(<0.5d)/M(0.5-2d)/L(>2d)
- 状态：全部 checkpoint 已完成；**3 个 P0 已全部修复并上线验证**（2026-07-06 当日）

## ✅ P0 修复记录（当日闭环）

| P0 | commit | 修复与验证 |
|---|---|---|
| CP1-1 NER 后端配置越权 | 9b09673 | 路由改 require_super_admin + host 白名单；test_role_matrix 19 passed（user/operator/reviewer PUT→403） |
| CP3-1 lb_proxy 无健康检查 | f1c1198 | 死上游冷却摘除+半开恢复+跨上游重试+/__lb_status；**杀 8130 后 8/8 请求仍 200**（此前~50% 失败），恢复自动回流；LB 测试 7 passed |
| CP0-5/CP5-1 JWT 明文 | 41b376e | 外置 ~/.redaction_secrets(600)+fail-fast+SECRETS.md 轮换 runbook；仓内无明文残留；重启 health 绿、401 仍强制。⚠️**值未轮换**（演示中，低峰按 runbook 轮换一次即闭环；注：明文曾入 git 历史，仓库本地未 push，轮换后历史值作废）

---

## CP0 基线与卫生

### 自动化基线（好消息）

| 项 | 结果 |
|---|---|
| ruff（E,F,W,I,N,UP,B） | backend/app 全绿 |
| tsc --noEmit | 0 错误 |
| eslint | 0 错误 / 4 个 `react-hooks/exhaustive-deps` 警告 |
| 前端 any/@ts-ignore/console.log/TODO | 全部 0 |
| pip check（.venv） | 通过 |
| 仓内泄密扫描 | 干净（仅枚举名误报；deploy/ 中 JWT 已脱敏为占位符） |
| 全量 pytest（服务器 dataInfra env） | **296 passed / 2 skipped / 0 failed**（修复测试目录漂移后；修复前 4 failed + 133 个用例根本没被部署） |

### CP0 发现

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP0-1 | **P1** | **本地开发环境无法运行测试套件**：仓库 .venv 收集 backend/tests 直接 38 个 import 错误；测试只能在服务器 dataInfra conda 环境跑。新人/CI 本地复现无路径，且 requirements.lock 与真实测试环境未对齐验证 | `python -m pytest backend/tests` 于 .venv → 38 collection errors | 提供 `requirements-dev.txt`+一键 `make test`（或 README 步骤），CI 用同一锁文件建环境 | M |
| CP0-2 | **P2** | backend 包根散落 16 个未跟踪实验脚本（bank_ph.py、cust_pii.py、run_*.sh、tmp_render.py 等），污染包命名空间且有误 import 风险 | `git status --short`（backend/ 根） | 移入 `backend/experiments/`（gitignore）或删除 | S |
| CP0-3 | **P2** | e2e/.artifacts 测试产物混入版本库并持续漂移（2 个已跟踪文件被修改 + 8 个新产物未跟踪） | `git status`：golden_batch_fail.png M 等 | `.gitignore` 加 `e2e/.artifacts/`，已跟踪的移出索引 | S |
| CP0-4 | **P2** | 3 个 stale `frontend/dist.bak-*` 目录（dual/flip/npu 时代备份）留在工作区 | frontend/ 目录列表 | 删除（历史在 git；服务器另有部署副本） | S |
| CP0-5 | **P1** | **服务器运行脚本含明文 JWT 密钥**（~/backend_g0.sh，deploy/ 仓内副本已脱敏但服务器实机未收敛）；GLM API key 曾出现在会话脚本（未入仓 ✓） | 服务器 `grep JWT ~/backend_g0.sh` | 密钥外置：`~/.redaction_secrets` env 文件（chmod 600）由脚本 source；仓内文档说明轮换流程 | S |
| CP0-6 | **P1** | **测试从未被系统性执行**：服务器 tests/ 落后本地多个版本（133 个用例缺失、1 个断言仍引用 6/18 已删除的 MED_INSTITUTION）；无 CI 门禁执行 pytest（历次部署只推产品文件）。审计中已同步并修至 296 全绿，但流程缺口未堵 | 修复前：服务器 pytest 165 collected/1 failed；本地 .venv 无法收集（CP0-1） | 部署脚本把 tests/ 纳入同步；CI（或部署前钩子）强制 `pytest -q` 全绿才可发布 | M |
| CP0-7 | **P1** | **部署工件三副本漂移**：`~/lb_proxy.py`（运行版=least-inflight）/ `deploy/host-scripts/lb_proxy.py`（=运行版）/ `deploy/dual-5090/lb_proxy.py`（6/24 旧轮询版）；host-scripts/ 整目录 40+ 文件是第二套全量脚本副本，含 magi/LA 时代化石，与 dual-5090/ 各自半新半旧 | `diff` 三方（审计日志）；tests 指向 host-scripts | 收敛为**单一规范目录**（建议 host-scripts），dual-5090 只留 README 指引；部署脚本从规范目录分发，禁手工双写 | M |
| CP0-8 | **P2** | pyproject 配置了 bandit 但任何环境都未安装（服务器/本地均 No module named bandit），安全静态扫描形同虚设 | `python -m bandit` 两侧均失败 | dev-requirements 固化 bandit，纳入 CI | S |

---

## CP1 接口层（api/，142 端点 × 13 路由）

**整体强项（已核实）**：路由级 `Depends(require_auth)` + 端点级 owner 校验（assert_file_owner/assert_job_owner）双层模式，files/jobs/redaction/structured 对象级越权防护覆盖完整；路径遍历有 safe_path_in_dir+卷名白名单+resumable id 正则三重防护；**无经典 IDOR/未授权下载 P0**。短板在"全局配置的权限分级"与接口卫生。

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP1-1 | **P0** | **NER 后端 URL 配置可被任意已认证用户改写**：`ner_backend` 路由仅 `require_auth`（main.py:447），而功能同类的 model_config 是 `require_super_admin`（main.py:446）——明显分级遗漏。普通用户 PUT llamacpp_base_url 到攻击者服务器 → 全租户文档正文送外部（外泄）+ 返回空实体（"已脱敏"导出实为原文，**静默漏遮**）；兼 SSRF | ner_backend.py:52/59/71；main.py:446 vs 447 | 路由改 `require_super_admin`；base_url 加 host 白名单 | S |
| CP1-2 | **P1** | `/auth/register` 无鉴权，匿名者仅 5/min 限流即可自助开出完整流水线权限的 user 账号，削弱"已认证"信任边界并放大 CP1-1 | auth.py:195-204；main.py:439（auth router 无 router 级依赖） | 改邀请/审批开户或加默认关闭开关（**待 Tracy 确认是否产品预期**） | M |
| CP1-3 | **P1** | 结构化数据源 test/create 对用户任意 host:port 外连（SSRF/内网端口探测），test 把 str(exc) 放进 200 响应泄露内部拓扑 | structured.py:110-120,123-138 | 出站白名单/网络隔离；错误归一化 | M |
| CP1-4 | **P1** | 内部绝对路径泄露：get_file_info 无 response_model 直回含 file_path/output_path 的原始记录；/safety/storage-info 回目录 realpath | file_management_service.py:719-725；files.py:929-935；safety.py:71-78 | 加白名单 response_model 剥离物理路径 | S |
| CP1-5 | **P1** | structured `_error()` 一律 HTTPException(4xx, detail=str(exc))，4xx 不经全局 5xx 遮蔽器，服务层 ValueError 文案原样返回客户端 | structured.py:45-46 及 78/136/220/285 | 对外固定文案，str(exc) 仅日志 | M |
| CP1-6 | **P1** | 批量导出 file_ids 无上限（仅 min_length=1），estimate/export 逐个 get+stat，export 无限流 | file_schemas.py:106-113；files.py:390-402,405-426 | file_ids 加 max_length(5000)；export 挂限流 | S |
| CP1-7 | **P2** | 断点续传分块 PUT 无会话锁：并发同 offset 两块都过校验并 append，靠 complete 整体 size 兜底 | files.py:683-719 | 每 upload_id 加 asyncio.Lock，锁内复核 offset | M |
| CP1-8 | **P2** | 过期分块仅惰性清理，partial/<owner>/ 不被周期 orphan cleanup 扫描（只扫顶层），残块占盘 | files.py:605-617,641；main.py:99-138 | partial/ 纳入周期递归清理 | S |
| CP1-9 | **P2** | 三处 assert_job_owner 在 try 外，NotFoundError（ValueError 子类）无全局处理器→越权/不存在返 500 而非 404（契约不一致，不泄数据） | jobs.py:256,293,580；main.py:319-321 | try 包裹映射 404 或注册全局处理器 | S |
| CP1-10 | **P2** | /health/services 无鉴权，向匿名者暴露磁盘余量/GPU 显存/备份库名与新鲜度 | main.py:504-505（注释称探针有意公开，**待复核**） | 加 require_auth 或裁字段 | S |
| CP1-11 | **P2** | 多变更类审计缺 user= 主体（create job/batch export/structured 增改） | jobs.py:155；files.py:425；structured.py:79/137/360 | 统一透传 user=owner_id | S |

## CP2 服务层与算法工程面

### 验证为健康的项（不修）

- 后端 OCR 文本块缓存：OrderedDict + TTL + MAX_ITEMS 双界（ocr_cache.py:30,212,259）✓
- HaS 同步 httpx.Client 的调用已全部经 `asyncio.to_thread` 派发（has_text_analysis.py:286,358），不阻塞事件循环 ✓；httpx.Client 线程安全 ✓
- NER 缓存有 TTL+条数上限（has_client.py:128-129）✓

### 发现

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP2-1 | **P1** | **单例跨租户竞态**：orchestrator 把 `owner_id` 写到 OcrHasVisionService 单例属性（redaction_orchestrator.py:461），detect 流程若干 await 之后才在正则兜底读取（ocr_has_vision_service.py:252 `get_regex_types(self.current_owner_id)`）；JOB_CONCURRENCY=4 下并发不同租户任务会互相覆盖 → **A 租户文档套用 B 租户自定义正则规则**（正确性+隔离双重问题）。同类 last_* 属性（last_ocr_blocks/last_duration_ms）读点紧跟 await 后尚安全，但模式脆弱 | 上述两处 file:line | owner_id 改为显式参数沿调用链传递（detect_and_draw 增参）；last_* 改为返回值携带 | M |
| CP2-2 | **P2** | httpx.AsyncClient 每请求新建 ×3（locate_grounding：_post_detect/_post_chat/_detect_has_image）+1（model_config_service），每次调用付 TCP 建连成本；30 页/分 × 每页 2-3 调用的常态下纯浪费 | `grep httpx.AsyncClient` 计数 | 模块级共享 AsyncClient（glm_visual_server.py 已是此模式，照抄） | S |
| CP2-3 | **P1** | **环境开关矩阵无启动校验**：VISUAL_SINGLE_CALL/VISUAL_TILE_RETRY/HAS_IMAGE_URL/ABSORB_SIGNATURES_IN_SEALS 与视觉后端拓扑强耦合，但互斥/依赖关系无 fail-fast（GLM 拓扑配 ABSORB=1 会吞真签字、LA 拓扑配 SINGLE_CALL=1 会召回坍塌——全靠人记）。当日实证：repro 少带 env 即"全通道假故障" | config.py 新增四开关无 validator | Settings 加 model_validator：按 VISUAL_FEATURES_MODEL_NAME 推断后端并校验开关组合，非法组合启动即报错并打印当前拓扑摘要 | S |
| CP2-4 | **P1** | **核心路径零单测**（联动 CP0-6）：redaction_orchestrator、redactor、vision_service、job_store、ocr_has_vision_service 无任何测试；而融合层三谓词（hull 合并/YOLO 优先/跨行拆分）全是纯函数，是最低成本高价值的测试切入点 | 盘点比对 | 最小测试集：①vision_service 融合谓词表驱动单测（今天的真实坐标做 fixture）②job 状态机转移表③redactor 掩码渲染 golden 图比对 | M |
| CP2-5 | **P2** | 死代码/暂存项台账（GLM 拓扑跑稳一周后清）：locate_grounding 分块复检段+_TILE_RETRY 常量、_absorb_signatures_in_seals、ocr_pipeline.py 194 名再导出层（新代码应直 import 子模块）、la_*.sh/la_lm_*.sh、host-scripts 中 magi_*/sage_*/mk_* 化石 | 各文件 | 专项清理 commit，配全量回归 | M |
| CP2-6 | **P2** | 巨石文件：job_management_service.py 1757 行、vision_service.py 1662 行；后者天然接缝已存在（融合谓词层 ~500 行可平移成 vision_merge.py，零行为变化） | 行数统计 | 按接缝拆分，只动 import 不改逻辑 | M |

## CP3 模型微服务与运维

## CP3 模型微服务与运维

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP3-1 | **P0** | **lb_proxy 无健康检查**：上游死亡后仍被轮询，请求直接报错（当日 la_g0 下线时 LB 9090 实证 "All connection attempts failed"）；least-inflight 还偏爱死上游（其 inflight 恒 0 最小） | deploy/host-scripts/lb_proxy.py（全文无探活/摘除） | 被动摘除：连接类异常→该上游进入冷却窗（半开重试探针恢复）；/health 端点聚合报各上游状态；~40 行改动 | S |
| CP3-2 | **P1** | 服务无声死亡无告警：vl_serve_g0 当日 clean-shutdown 一次（SIGTERM 来源未查明），LB 兜底掩盖，靠人工 nvidia-smi 才发现 | ~/logs/vl_serve_g0.log 当日记录 | 用户级 watchdog（cron 每分钟全服务端口+/health 探测，DOWN→拉起+事件落盘）；后端 /health/services 已有的面板加"重启计数"展示 | M |
| CP3-3 | **P1** | start_all 全冷启竞态：多 vLLM 并发抢显存时 HaS KV 分配失败，**脚本仍 exit 0**；需二次 start_all 手工补拉（当日两次复现） | start_all.sh（launch 后仅两处 wait） | 每个 vLLM launch 后逐一 wait ready（顺序化启动）；任一服务 fail 则非零退出并打印诊断 | S |
| CP3-4 | **P1** | 孤儿 EngineCore 清理纯手工：proctitle 覆盖 argv 致 pkill 失效，只能按显存指纹人肉杀（当日 4+ 次） | 当日操作记录 | stop_all.sh：按"预期进程清单"对卡上 compute-apps 做差集清理；服务脚本落 PID 文件 | S |
| CP3-5 | **P1** | ~/logs 无轮转（vLLM 日志无限增长写满盘风险）；无请求级 metrics；GPU/显存/服务状态无时序采集 | 服务器 ~/logs 目录 | 轮转：launch 包装 size-based 截断（或用户级 logrotate）；metrics 最小集=lb_proxy 记 per-upstream 计数/时延分位 + 每分钟 nvidia-smi 采样落 CSV | M |
| CP3-6 | **P1** | 部署工件多副本漂移（详见 CP0-7）：host-scripts 与 dual-5090 双目录、服务器 ~ 家目录为事实真相源，tests 目录曾漂移 133 用例 | CP0 证据 | 单一规范目录 + 同步脚本（rsync 清单式），部署即校验 md5 | M |
| CP3-7 | **P2** | glm_visual_server 复审：adapter 对 GLM 的调用超时用全局 300s（应对齐 VISUAL_FEATURES_TIMEOUT 量级）；无并发护栏（依赖 vLLM max-num-seqs 排队，可接受但未文档化）；prompt 已锚定消融版注释 ✓ | scripts/glm_visual_server.py:72 | 超时参数化；docstring 补并发模型说明 | S |
| CP3-8 | **P2** | YOLO 服务（has_image_server）恢复自 git 历史：健康检查✓ GPU 探测✓，但无 imgsz 参数暴露（固定默认 640——当日实测 1280 召回更优且仅 121ms） | scripts/has_image_server.py `_predict_sync`（无 imgsz） | 加 HAS_IMAGE_IMGSZ env（默认 1280），A/B 后切换 | S |

## CP4 前端

**整体强项（已核实）**：tsc/eslint 全绿；评审数据加载竞态处理扎实（AbortController + 序列号双护栏）；**i18n 零漂移**（zh/en 各 2294 key 双向缺失 0——盘点期 142 差异是多行 value 提取假象，已推翻）；ErrorBoundary 根+路由两级覆盖。**无 P0**。核心债在状态一致性。

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP4-1 | **P1** | jobs 资源三处独立缓存（use-jobs localStorage / batch-hub localStorage / job-detail state），完全绕过 react-query，mutation 无跨视图失效→删改后其他视图脏读至 TTL(30s)/轮询 | use-jobs.ts:80-130；use-batch-hub.ts:117；jobsApi.ts:599(deleteJob 无 invalidate) | jobs 统一纳入 react-query，mutation onSuccess 失效 | L |
| CP4-2 | **P1** | job-detail 轮询用全量 getJob（未传 performance:false），2s 一次；注释自述万级 job 详情达 38MB | job-detail-page.tsx:128,160-176 vs jobsApi.ts:556-564 | 轮询走 performance:false | M |
| CP4-3 | **P1** | URL 水合 useEffect ~277 行/17 依赖（wizard 拆分后仅剩的真接缝），频繁重跑难推理 | use-batch-wizard.ts:389-666 | 抽 useBatchHydration 收窄依赖 | L |
| CP4-4 | **P1** | 契约漂移信号：normalize 层同读两套后端形状（progress.pending‖pending_count）+legacy job_type 映射，大量字段兜底=FE/BE schema 分叉 | jobsApi.ts:206-231,285-287 | 固定单契约，dev 加 zod 运行时校验 | M |
| CP4-5 | **P1** | jobs 列表亚秒轮询（900ms）+相邻页预取，有活跃 job 就持续→规模化后端压力 | constants/timing.ts:19；use-jobs.ts:491-495,371 | 指数退避，基线提到 2-3s | S |
| CP4-6~16 | **P2** | 双重复 config 自动保存 effect / 错误归一化不一致(authFetch 抛裸 Error) / 导出轮询无超时 / 6 处 exhaustive-deps 抑制(背景说4) / AbortController 未透传给 batchGetFileRaw / use-jobs 774 行 8 职责 / 编辑态与列表计数瞬时不一致 / VISION_TIMEOUT 900s 无取消 / 轮询签名 O(n) 抖动 / 拦截器解包静默陷阱 / fetchJobDetails 无 mounted 护栏 | 见 CP4 代理明细（jobsApi/api-client/use-batch-wizard 等） | 逐条改进，改动量均小 | S-M |

## CP5 安全与数据面

### 验证为健康的项（不修）

- 路径穿越防御：`_validate_path` = realpath 归一 + UPLOAD/OUTPUT 白名单（file_parser.py:51），`../` 与符号链接均被 realpath 消解 ✓
- `/uploads` `/outputs` 刻意不挂 StaticFiles（main.py:434 注释明示防绕过 require_auth），走带鉴权的 FileResponse 处理器 ✓
- 孤儿文件清理器（main.py:99）：年龄阈值 + "有盘无引用即熔断跳过"防误删迁移事故 + file_store∪job 双源引用表——设计良好 ✓（当日审计用测试文件被删属按设计工作；测试夹具应放 uploads 外）
- 仓内无明文密钥（CP0 扫描）✓

### 发现（部分，对象级越权与 JWT 细节见 CP1 代理结果并入）

| ID | 级别 | 发现 | 证据 | 建议 | 工作量 |
|---|---|---|---|---|---|
| CP5-1 | **P1** | 服务器实机密钥明文（联动 CP0-5）：backend_g0.sh 内 JWT_SECRET_KEY；密钥自 6/13 起未轮换 | 服务器脚本 | 外置 env 文件 chmod 600 + 轮换 runbook（老 token 失效公告） | S |
| CP5-2 | **P2** | 备份文件（data/ 下 jobs/auth/jwt_secret 等 json 备份）明文落盘且无权限收紧记录；同机多租户（共享服务器）下属敏感面 | /health 输出 backup 段 | 备份目录 chmod 700；敏感 store（auth/jwt）加密或至少文档标注威胁模型 | M |

## CP6 汇总

### 六维健康度

| 维度 | 评分 | 一句话 |
|---|---|---|
| 算法/视觉链路 | ★★★★★ | GLM+VL+YOLO 融合 25/25，谓词全结构化无魔法数字，今日已达标 |
| 接口设计 | ★★★★☆ | 越权/路径防护成熟；短板在全局配置分级（CP1-1 P0）与错误细节泄露 |
| 鲁棒性 | ★★★☆☆ | 应用层缓存/防误删设计好；运维层无健康检查/无告警/冷启竞态（CP3 集中） |
| 可维护性 | ★★★☆☆ | ruff/tsc 全绿、i18n 零漂移是亮点；但核心零单测+两端巨石+部署多副本漂移 |
| 性能 | ★★★★☆ | OCR/NER 缓存有界、to_thread 派发正确；扣分在前端亚秒轮询+全量拉取、httpx 每请求新建 |
| 安全/数据面 | ★★★☆☆ | 路径/越权强；扣分在 CP1-1 P0 + 明文密钥 + SSRF 面 + 内部路径泄露 |

### 全项目 P0（3 条，交付前必修）

1. **CP1-1** NER 后端配置越权改写（SSRF+跨租户外泄+静默漏遮）— ner_backend 路由改 super_admin，S
2. **CP3-1** lb_proxy 无健康检查，死上游持续报错 — 被动摘除+探活，S
3. **CP0-5/CP5-1** 服务器明文 JWT 密钥未收敛未轮换 — env 外置+轮换，S

（三条都是 S 工作量，半天内可清零）

### P1 优先路线（按"价值/成本"排序）

| 序 | 条目 | 维度 | 量 |
|---|---|---|---|
| 1 | CP2-3 env 开关启动校验（治当日假故障根类） | 可维护 | S |
| 2 | CP3-3 start_all 顺序化+失败非零退出 | 运维 | S |
| 3 | CP1-4/CP1-5/CP1-6 路径泄露+错误泄露+导出上限 | 安全/接口 | S×3 |
| 4 | CP3-4 stop_all 系统化清孤儿 + CP3-2 watchdog | 运维 | S+M |
| 5 | CP2-1 单例跨租户竞态改显式传参 | 鲁棒性 | M |
| 6 | CP0-6 CI 门禁 pytest + CP0-7 部署单一真相源 | 流程 | M×2 |
| 7 | CP4-1 jobs 统一 react-query + CP4-5 轮询退避 | 前端 | L+S |
| 8 | CP2-4 核心路径最小单测集（融合谓词优先） | 测试 | M |
| 9 | CP1-2 开放注册收敛（待产品确认） | 安全 | M |

### P2

死代码清理（CP2-5，GLM 拓扑稳定一周后）、巨石拆分（CP2-6/CP4-3）、卫生（CP0-2/3/4/8）、前端一致性长尾（CP4-6~16）。

### 审计副产品（已当场修复）

- 服务器 tests/ 漂移修复：133 缺失用例补齐 + lb_proxy 规范副本同步 → **pytest 296 全绿**（此前部署从未系统跑过测试）
- 发现即修不越权原则：仅同步测试与规范脚本，未动任何产品逻辑

### 结论

算法链路已达企业级；**工程交付差距集中在三处 S 级 P0（半天可清）+ 运维可观测性缺失（CP3 一组）+ 测试/流程门禁**。建议顺序：先清 3 个 P0 → 补 lb 健康检查与 start_all 顺序化（运维止血）→ env 校验与错误/路径泄露（接口收口）→ CI 门禁 → 前端 jobs 状态统一。整体无推倒重来项，均为增量收口。
